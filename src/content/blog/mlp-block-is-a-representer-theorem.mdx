---
title: "The MLP Block Is a Representer Theorem"
seoTitle: "The MLP Block Is a Representer Theorem"
description: "After the 3Blue1Brown attention video you can read half a transformer: you can see which token attends to which. The other half, the MLP block, stays a black box. But attention is legible because it is a kernel, a vote by similarity, and if you make the MLP a kernel too, its output becomes the same thing: a representer-theorem vote over learned prototypes. Then the whole transformer explains itself."
seoDescription: "Attention is legible because it is a kernel. Make the MLP a kernel and its block becomes a representer-theorem vote over prototypes, legible too."
pubDate: 2026-06-27
companion: mlp-block-is-a-representer-theorem-jax-flax-nnx
draft: false
tags: [ml, kernels, transformers, attention, mlp, interpretability, representer-theorem, yat, deep-learning, theory]
references:
  - authors: "Sanderson, G. (3Blue1Brown)"
    year: 2024
    title: "Attention in Transformers, and How Might LLMs Store Facts (Deep Learning, Chapters 6-7)"
    venue: "3blue1brown.com"
  - authors: "Schölkopf, B., Herbrich, R., Smola, A. J."
    year: 2001
    title: "A Generalized Representer Theorem"
    venue: "COLT 2001"
  - authors: "Yeh, C., Kim, J., Yen, I. E., Ravikumar, P."
    year: 2018
    title: "Representer Point Selection for Explaining Deep Neural Networks"
    venue: "NeurIPS 2018"
  - authors: "Elhage, N., et al."
    year: 2022
    title: "Toy Models of Superposition"
    venue: "Anthropic"
  - authors: "Geva, M., Schuster, R., Berant, J., Levy, O."
    year: 2021
    title: "Transformer Feed-Forward Layers Are Key-Value Memories"
    venue: "EMNLP 2021"
  - authors: "Bouhsine, T."
    year: 2026
    title: "A Universal Reproducing Kernel Hilbert Space from Polynomial Alignment and IMQ Distance"
    arxiv: "2605.03262"
keywords:
  - MLP block representer theorem
  - transformer interpretability kernel
  - attention is a kernel
  - Yat kernel MLP explainable
  - representer point attribution
---
import MemoryMap from '../../components/viz/MemoryMap.astro';
import RepresenterAttribution from '../../components/viz/RepresenterAttribution.astro';
import EditKnob from '../../components/viz/EditKnob.astro';
import AbstainMeter from '../../components/viz/AbstainMeter.astro';

If you watched the [3Blue1Brown attention video](https://www.3blue1brown.com/lessons/attention), you came away able to read half of a transformer. You can point at the attention pattern and say what it does: this token is looking at that one, the verb is reaching back for its subject, the pronoun is fetching its referent. The picture is legible because you can see who is talking to whom.

Then the same series reaches the other half, the MLP block, and the tone changes. This is where the network stores what it knows, and the honest answer for how it stores it is that nobody is quite sure. The neurons are polysemantic, the features are superposed, the block is a wall of numbers with no handle on it. So you finish the series able to read one half of the machine and staring at the other. **Why is attention the readable half and the MLP the black box?**

## Attention reads because it is a kernel

The asymmetry is not about depth or width; it is about the primitive. Attention is built from a similarity. Each token forms a query, every other token a key, and the output is a weighted average of values where the weights are how similar the query is to each key. That is a kernel, and a vote by similarity is something you can always read: the weights *are* the explanation, one number per source token, all of them adding to one. We made this exact in [Attention Is Explainable Because It Is a Kernel](/blog/attention-is-a-kernel/). The legibility was never a happy accident of attention; it is what any kernel gives you, because a kernel says "here is how much each reference counts," out loud.

So the real question sharpens. The MLP is opaque, but is its opacity fundamental, or is it just that the MLP was built from the wrong primitive?

## The MLP is opaque because its primitive has no reference

It is the wrong primitive. A standard hidden unit computes $\sigma(\langle w, x\rangle + b)$, and the weight $w$ in there is not a reference you can point at; it is a direction, a hyperplane normal, and the unit fires across an entire half-space of inputs. There is no center, no "this is what I am looking for," nothing to put your finger on, which is exactly [where a weight cannot live](/blog/where-does-a-weight-live/): off in a space of directions, joined to the data only by a shadow. Superposition and polysemanticity are downstream of that one fact. The primitive carries no geometry, so the block it builds carries none either, and there is nothing for an explanation to grab.

Attention got a metric for free, because a similarity has one built in. The MLP threw the metric away when it chose an activation. So the fix writes itself: give the MLP a kernel too.

## Replace the activation with a kernel

Swap the activation for the [Yat kernel](/blog/what-a-finite-kernel-buys-an-mlp/) and the hidden unit stops being a direction and becomes a similarity to a learned center $W_u$:

$$
\mathrm{yat}(x;\, W_u) = \frac{(\langle x, W_u\rangle + b)^2}{\lVert x - W_u\rVert^2 + \varepsilon} .
$$

Now the unit has a reference, the center $W_u$, a point in the same space as the input, and it fires when the input is near and aligned with it. The hidden layer is no longer a wall of numbers; it is a vector of similarities to a handful of named prototypes. The same swap that makes attention's weights readable has been applied to the other half of the block.

<MemoryMap client:visible caption="The real feed-forward memory of a small transformer trained on Shakespeare, running live in your browser. Every cell is one slot, decoded to the token it writes into the stream. Type a prompt and the slots fire: a cell brightens with its real kernel weight k(Wᵤ, x) at your last character. The block is no longer a slab of numbers, it is a memory you can read." />

## The block is a representer theorem

Here is the part that turns "more readable" into "completely readable." The MLP block does not stop at the hidden layer; it applies a linear projection on top, $y = W_2 \,\mathrm{yat}(x)$. Write that sum out, with $\alpha_{u} = (W_2)_{:,u}$ the output row for unit $u$, and the whole block collapses to

$$
y = \sum_{u} \alpha_{u}\, k(W_u,\, x) ,
$$

a signed, weighted sum of kernel evaluations against the centers. That is exactly the form the **representer theorem** promises. The theorem says the optimal weight in a reproducing kernel Hilbert space is always a combination of kernel evaluations at a set of centers, $f(\cdot) = \sum_i \alpha_i\, k(x_i, \cdot)$, which is the whole subject of [Where Does a Weight Live?](/blog/where-does-a-weight-live/). The kernelized MLP block is that form, made the architecture.

It is worth being exact about the difference, because the difference is the point. The representer theorem fixes its centers to the training data and proves that the *optimum* lands in their span. The Yat block does not wait for the optimum and does not fix the centers to the data: it bakes the representer form in and lets the centers $W_u$ be learned. So every MLP block, at every step of training, is already a representer-theorem vote, a sum over prototypes you can read, where the prototypes themselves are what learning moves around. The decomposition is not an after-the-fact attribution method bolted onto a black box, the way [representer-point selection](https://arxiv.org/abs/1811.09720) wraps a trained network; it is what the block literally computes.

<RepresenterAttribution client:visible caption="The same model, predicting the next character live. Its feed-forward output is exactly Σᵤ k(Wᵤ, x) vᵤ, so we split the prediction into the slots that wrote it: each row is a real memory slot, the tokens it writes, how hard your prompt fires it, and its signed push toward the answer. The reconstruction is exact because the decomposition is the block's arithmetic. Type any prompt and watch the vote recompose." />

## Both halves, one principle

Set the two halves of the transformer side by side and they are the same equation. Attention produces a token's output as $\sum_j a_j\, v_j$, a vote over the other tokens' values with weights $a_j$ that are similarities. The Yat MLP produces its output as $\sum_u \alpha_u\, k(W_u, x)$, a vote over learned prototypes with weights that are similarities. One votes over the tokens present in the context, the other over concepts the model has stored, but both are kernels, and both wear their explanation on the outside: a list of references and how much each one counted.

## What a white box buys you

Legible is not the end of it; legible is the beginning. Once the block is $\sum_u k(W_u, x)\, v_u$, with $v_u = (W_2)_{:,u}$ the vector it writes into the residual stream, the feed-forward layer is a **key-value memory**: keys $W_u$ you can locate, values $v_u$ you can decode, weights that say how much each one fired. That is the same picture researchers reached for the FFN the hard way, calling its rows key-value memories and probing them with statistics; the kernel makes it exact rather than a useful approximation. And an exact memory is one you can operate on.

You can **read** a slot: decode its value $v_u$ through the unembedding and it tells you, in tokens, what it writes ("this one ends lines," "this one starts capitals"). You can **attribute** an output: since it is a sum, you split any FFN output into the slots that produced it, with no gradients and no saliency guesswork, because the decomposition is the arithmetic. That is what the memory map and the attribution panel above already showed, on the real model.

The next two go further, from reading the memory to operating on it. You can **edit** a single slot: turn one $v_u$ up or down and, because the output is linear in $k(W_u, x)$, the generation changes the way its label predicts, model surgery with a screwdriver instead of a sledgehammer.

<EditKnob client:visible caption="White-box surgery on the real model. Pick a readable slot (the one that writes newlines, say), turn its gain up, and the model generates live with that one value vector amplified. The left side is untouched; on the right, the amplified slot floods the stream with exactly the token it was decoded to write. One slot, one knob, a predictable effect, run live in your browser." />

And you can **tell when the memory is out of its depth**: the Yat kernel is local, so an input near nothing it has stored lights up no slot and the block writes almost nothing, an abstention signal you read straight off the weights.

<AbstainMeter client:visible caption="The same model, with each character tinted by its real peak memory weight, maxᵤ k(Wᵤ, x). Real Shakespeare glows: the memory recognizes it. Type gibberish and the characters go dark: no slot fires, the block writes nothing, and the model is telling you, in the kernel weights themselves, that it is out of its depth. Try it both ways." />

None of that is available when the primitive is a direction with no reference. All of it falls out the moment the primitive is a kernel.

That is the whole argument. The MLP was never doomed to be a black box; it was built from a primitive with no metric, and the opacity followed from that and nothing else. Give it a kernel and the block becomes a representer-theorem vote over prototypes, a key-value memory you can read, attribute through, edit, and trust to abstain, as legible as the attention beside it. The thing that made half the transformer readable, a similarity to a reference you can name, was available for the other half all along.

---

*Want to run it? The [JAX/Flax NNX companion](/blog/mlp-block-is-a-representer-theorem-jax-flax-nnx/) builds a real transformer with a Yat feed-forward block, trains it on tinyshakespeare, and then reads, attributes, edits, and probes its memory directly, the four white-box payoffs in code.*

*The attention and MLP framings are from [3Blue1Brown's deep learning series](https://www.3blue1brown.com/topics/neural-networks); the representer theorem is [Schölkopf, Herbrich and Smola (2001)](https://link.springer.com/chapter/10.1007/3-540-44581-1_27); representer-point attribution is [Yeh et al. (2018)](https://arxiv.org/abs/1811.09720); feed-forward layers as key-value memories is [Geva et al. (2021)](https://aclanthology.org/2021.emnlp-main.446/); superposition in MLP blocks is [Elhage et al. (2022)](https://transformer-circuits.pub/2022/toy_model/index.html); the Yat kernel is [Bouhsine (2026)](https://arxiv.org/abs/2605.03262). It builds on [Attention Is a Kernel](/blog/attention-is-a-kernel/) and [What a Finite Kernel Buys an MLP](/blog/what-a-finite-kernel-buys-an-mlp/).*

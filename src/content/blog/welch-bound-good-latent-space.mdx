---
title: 'What Makes a Good Latent Space? The Welch Bound and the Simplex'
description: 'A visual story about the hidden codebook inside representation learning: why collapse happens, why opposition is a trap, why class means form a simplex, and why the Welch bound from radio engineering tells us the best possible geometry when too many concepts share too few dimensions.'
pubDate: 2026-06-02
tags: [ml, contrastive-learning, representation-learning, welch-bound, frame-theory, neural-collapse, simplex-etf, tight-frames, latent-space, packing]
keywords:
  - Welch bound
  - equiangular tight frame
  - simplex ETF
  - frame potential
  - Grassmannian packing
  - neural collapse
  - contrastive learning
  - alignment and uniformity
  - latent space geometry
  - tight frame
  - coherence
  - effective rank
  - representation learning
  - spherical codes
---
import ConeEffect from '../../components/viz/ConeEffect.astro';
import LatentSpaceBirth from '../../components/viz/LatentSpaceBirth.astro';
import AlignmentPull from '../../components/viz/AlignmentPull.astro';
import MarginRobustness from '../../components/viz/MarginRobustness.astro';
import RankIsotropy from '../../components/viz/RankIsotropy.astro';
import OppositionVsOrthogonality from '../../components/viz/OppositionVsOrthogonality.astro';
import SimplexPacking from '../../components/viz/SimplexPacking.astro';
import SpherePacking3D from '../../components/viz/SpherePacking3D.astro';
import SignatureInterference from '../../components/viz/SignatureInterference.astro';
import FrameRaces from '../../components/viz/FrameRaces.astro';
import AlignUniformity from '../../components/viz/AlignUniformity.astro';
import CiteAs from '../../components/CiteAs.astro';

*A latent space is a codebook.*

Every class, concept, image, caption, or document gets assigned a direction. At inference time, the model listens to those directions and decides which ones sound alike. If two directions are too correlated, they interfere. If every direction collapses into the same corner, the codebook is useless. If the codebook wastes dimensions, it becomes fragile.

That is the story of a good latent space: not "features" in the abstract, but a crowded communication channel. How many meanings can you pack into a fixed number of dimensions before they start talking over each other?

That question has a floor. Lloyd Welch proved it in 1974 for signal codes, long before anyone was training contrastive encoders. Modern representation learning keeps rediscovering the same object: when there is room, class means arrange as a **regular simplex**; when there is not, the best codebook presses against the **Welch bound**.

This post builds that geometry from the failure mode up.

## The mystery appears immediately

Start with a tiny encoder training live in your browser. Each colour is a class; each dot is an example; the larger rings are class centroids. Press **Play**.

With **repulsion on**, the classes tighten and spread into a clean codebook. Toggle **repulsion off**, re-roll, and run it again. The same encoder now takes the cheap solution: every class lands on the same point.

That pile-up is **collapse**. It is the first villain of representation learning.

<LatentSpaceBirth caption="A latent space forming under live training, slowed down so the path is visible. Left: the raw 2-D encoder output before L2 normalization; raise raw span to zoom out for the full motion, or lower it to zoom in on collapse. Right: the same points projected onto the unit circle, where the angular/simplex geometry lives. Colour = class, dots = examples, large rings = class centroids. Repulsion ON: same-class points tighten while class centroids spread into an even packing. Repulsion OFF: alignment remains, but there is no pressure between classes, so the normalized view collapses. With 3 classes on the circle, the centroids settle at the simplex angle: cosine -0.5, or 120 degrees apart." />

The interesting part is not merely that repulsion prevents collapse. The interesting part is the shape it chooses. Why do the centroids settle at those angles? Why not push every class to the opposite side of every other class? Why does the good solution look so rigid?

To answer that, we need three constraints. A good latent space must be tight, separated, and full-rank. The drama is that these constraints do not automatically agree.

## Constraint 1: make each class tight

Neural networks do not begin with an evenly spread sphere. Push data through a random, untrained ReLU network, normalize the outputs, and the points bunch into a cone. Add depth and the cone gets narrower.

<ConeEffect caption="Outputs of a random, untrained ReLU network, normalized to the circle. Left is the shallow baseline; right is the selected depth. The colours are fixed random input slices, not classes, so you can see the same population being squeezed into one angular cone as depth increases. The arrow marks the mean direction, the shaded wedge marks the cone, and the small histogram shows how concentrated the angles have become. Drag the depth slider and re-roll for a new random network." />

Training has to pull examples of the same class together. This is the first pressure: **alignment**.

<AlignmentPull caption="One class of points, with a slider for the strength of alignment. With no pull, it is a loose cloud. Increase the pull and the within-class variance shrinks toward zero: the class becomes a single point. Re-roll for a new cloud." />

Alignment is necessary, but it is not enough. If all the loss says is "matching things should be close," then every class can satisfy it by becoming the same point. Collapse is not a weird bug. It is the natural endpoint of alignment without a counterforce.

## Constraint 2: leave a margin

The counterforce is separation. But separation is not a decorative choice. It buys robustness.

Below, two noisy classes sit at an angle you control. Before dragging the slider, make a bet: at what angle does the same noise stop being dangerous?

<MarginRobustness caption="Two classes under live noise, classified by nearest centroid. The dashed line is the decision boundary; an x marks a point noise pushed onto the wrong side. Drag the angle between the classes: at a narrow angle the clouds overlap and accuracy falls; widen it and the same noise stops mattering. Separation is margin." />

The larger the angle, the more noise a point can absorb before crossing the boundary. In the codebook view, margin is low crosstalk: one class's signal should not be easily confused for another's.

So the naive goal seems obvious: maximize every pairwise angle.

That is where the story gets interesting, because the obvious goal is wrong.

## Constraint 3: do not waste dimensions

An embedding can look separated while secretly living in too few dimensions. If a 100-dimensional latent space collapses onto a line, 99 dimensions have become dead capacity. The codebook may have large distances along that line, but it has lost room to express independent distinctions.

The measurement to watch is **effective rank**: how many directions the cloud actually uses.

<RankIsotropy caption="The same cloud, with a slider that squashes its covariance. Round and isotropic: the ellipse is a circle, effective rank is about 2, and both dimensions are used. Squashed: the cloud collapses toward a line, effective rank approaches 1, and one dimension has been wasted. A good latent space keeps its covariance round. In frame theory, this equal-energy condition is called tightness." />

Now the conflict is visible. Separation wants large angles. Rank wants independent directions. Those are not the same thing.

## The false answer: make everything opposite

Here is the trap. On the cosine scale, -1 looks like "maximally different." But two antiparallel vectors span one line. They are the same direction with a sign flip.

Before you drag the green arrow, guess which configuration is more useful for a representation: 180 degrees apart, or 90 degrees apart?

<OppositionVsOrthogonality caption="Two unit vectors: drag the green tip. Antiparallel (cos = -1): maximally opposite, but the pair collapses onto a 1-D line. Orthogonal (cos = 0): independent, spanning the full plane. Watch the span readout drop from 2 to 1 at opposition. Pairwise distance alone can reward a rank collapse." />

Opposition wins a single pairwise contest by sacrificing a dimension. Orthogonality uses the space. For two classes, opposition is fine. For three or more, blindly chasing antipodes becomes impossible and, worse, conceptually misleading.

The correct question is not "how do I make every pair as opposite as possible?" It is:

**How do I make all pairs as evenly separated as possible while keeping the whole codebook full-rank?**

That question has a beautiful answer.

## If the classes fit, the answer is a simplex

For centered class means, $C$ classes fit as a perfect simplex when the embedding dimension satisfies $d \ge C - 1$. In that case, all pairs can share one common angle. No pair gets special treatment. No dimension is wasted.

The pairwise cosine is

$$
-\frac{1}{C-1}.
$$

That number is the fingerprint of the regular simplex. Three classes land 120 degrees apart. Four classes form a tetrahedron. As the number of classes grows, the cosine approaches 0: almost orthogonal, not opposite.

<SimplexPacking caption="The simplex angle, cos = -1/(C-1), as a function of the number of classes. C = 2 gives -1, the only case where opposition is optimal. C = 3 gives -0.5 (120 degrees); C = 4 gives -0.33; by C = 50 it is -0.02, essentially orthogonal. The simplex is the maximally even centered arrangement." />

This is the first payoff. The "good" centroids from the opening animation were not merely spreading out. They were moving toward the only centered arrangement where every class gets the same deal.

The rotating sphere below shows the same idea in 3-D. With room for orthogonal axes, the vectors can avoid interference entirely. With four centered class means, the tetrahedron appears. Add more vectors than dimensions and the sphere runs out of room.

<SpherePacking3D caption="C unit vectors on a rotating 3-D sphere, descending live on the frame potential Σ⟨p_i,p_j⟩². C <= 3 can become orthogonal. C = 4 forms a regular tetrahedron, the centered simplex. Push C beyond 3 and there is no room for orthogonality; the points are forced into a symmetric tight frame near the Welch bound. Drag C, re-roll, and compare realised coherence to the Welch floor." />

This is where the radio engineers enter.

## If they do not fit, Welch tells you the crosstalk floor

Imagine many users sharing one radio channel. Each user gets a signature vector. If two signatures are correlated, the receiver hears interference. You want as many users as possible, each with as little crosstalk as possible.

That was the signal-processing version of our latent-space problem. In 1974, [Lloyd Welch](https://cir.nii.ac.jp/crid/1363388846208246912) proved a lower bound on the maximum cross-correlation among such signals.

Play with the 2-D version. With two users, the signatures can be orthogonal. Add a third user, and zero interference is impossible.

<SignatureInterference caption="Welch's 1974 problem, shown in 2-D. Each user gets a unit signature vector; correlated signatures interfere. With N <= 2 users, the signatures can be orthogonal and interference can fall to zero. Add a third user and there is no more room: the interference cannot drop below the Welch floor, no matter how the signatures move. Press Play and drag N." />

For $C$ unit vectors in $d$ dimensions, with $C > d$, the worst absolute inner product must obey

$$
\max_{i \ne j} |\langle x_i, x_j\rangle|
\ge
\sqrt{\frac{C-d}{d(C-1)}}.
$$

That is the **Welch bound**. It says: once the codebook is overloaded, some amount of crosstalk is unavoidable. You can move the vectors around, train longer, tune the optimizer, or change the loss temperature. You cannot beat the floor imposed by the dimension.

When the floor is met, the vectors form an **equiangular tight frame**. Equiangular means the crosstalk is shared evenly instead of dumped onto a few unlucky pairs. Tight means the vectors use the whole space evenly. It is exactly the compromise the three constraints demanded.

Now the post's title should feel less like jargon. A good latent space is a good overloaded codebook.

## Why descent can actually reach the good codebook

There is still a practical worry. Maybe the best arrangement exists, but gradient descent almost never finds it.

The frame-potential story explains why that worry is smaller than it looks. Benedetto and Fickus introduced the frame potential as an energy for measuring how far a set of unit vectors is from being evenly spread; modern summaries of their theorem emphasize that this landscape has no spurious local minimizers for the tight-frame objective. In plain language: if you descend the right energy, the bad traps are not the main obstacle.

Watch six independent random starts descend together.

<FrameRaces caption="Six independent random initialisations of C points, all descending the frame potential. Each curve is one random start. They converge to the same Welch floor, C²/d - C, instead of getting trapped in separate bad minima. Press Play, re-roll, and drag C." />

The starts disagree. The floor does not. That is why the geometry is more than an existence theorem; it is an attractor.

## Neural networks rediscover the same shape

In 2020, [Papyan, Han, and Donoho](https://arxiv.org/abs/2008.08186) looked at classifiers trained past zero training error. The late phase of training simplified instead of becoming more chaotic:

- examples from the same class collapsed toward their class mean;
- class means converged toward a simplex equiangular tight frame;
- classifier weights aligned with those means;
- prediction approached nearest-class-mean classification.

They called this **neural collapse**.

Read through the codebook story, neural collapse is not a strange coincidence. The network has learned a low-crosstalk codebook for the classes. Within-class variation vanishes; class means arrange as evenly as the dimension allows; the classifier becomes a decoder for that codebook.

The surprise is not that neural networks found geometry. The surprise is that the geometry is so simple.

## Contrastive learning is codebook design with two forces

[Wang and Isola](https://proceedings.mlr.press/v119/wang20k.html) later gave contrastive learning a clean two-force description:

- **alignment:** matching views should be close;
- **uniformity:** normalized features should spread over the hypersphere.

That is the same plot with different names. Alignment tightens each class or positive pair. Uniformity prevents collapse and lowers crosstalk by using the sphere.

<AlignUniformity caption="A contrastive loss as two forces. Alignment pulls same-class points together. Uniformity spreads all points over the circle. Uniformity = 0: classes collapse together. Alignment = 0: the points spread out, but labels are intermixed. Both on: tight clusters arranged evenly. Drag the two sliders and the class count; press Play." />

Different contrastive methods build different machinery around those two forces: in-batch negatives, memory queues, temperatures, predictor heads, variance penalties, covariance penalties. Under the machinery is the same geometric demand:

**make matching things share a code, and make nonmatching codes interfere as little as the dimension allows.**

There is one wrinkle worth keeping. A naive pairwise cosine push treats -1 as the ideal value for every negative pair. That is the false answer from earlier. The good packing emerges because all pairs cannot be antipodal at once; the impossible pairwise request gets bent by the global constraint into a simplex or a Welch-bound frame. The geometry of the whole codebook matters more than the target for any one pair.

## The answer

A good latent space is not merely "well separated." It is a codebook satisfying three constraints at once:

- **tightness:** examples that should match collapse toward the same code;
- **margin:** different codes have enough angular room to resist noise;
- **rank:** the codes use the available dimensions instead of hiding on a line, plane, or cone.

When centered class means fit, the solution is the **regular simplex**, with pairwise cosine $-1/(C-1)$. When too many codes share too few dimensions, the best possible solution is an **equiangular tight frame** at the **Welch bound**.

The story is compelling because it is not just an ML story. Radio engineers ran into the same geometry while designing signatures for crowded channels. Frame theorists proved what the even configurations minimize. Classifiers rediscover the simplex during neural collapse. Contrastive learning balances alignment and uniformity to chase the same sphere-packing compromise.

The good latent space is the arrangement where the codes stop shouting over each other.

<CiteAs
  slug="welch-bound-good-latent-space"
  title="What Makes a Good Latent Space? The Welch Bound and the Simplex"
  pubDate="2026-06-02"
/>
